/* eslint-disable no-undef */
const webpack = require('webpack');
const path = require('path');
const DtsBundleWebpack = require('dts-bundle-webpack');

module.exports = {
  entry: './src/api/dms-player.ts',
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'dms-video-player.min.js',
    library: 'dmsVideoPlayer',
    libraryTarget: 'umd',
  },
  resolve: {
    extensions: ['.ts', '.tsx', '.js', '.css'],
  },
  devtool: 'none',
  module: {
    rules: [
      {
        test: /\.tsx?$/,
        loader: 'ts-loader',
        exclude: /node_modules/,
      },
      {
        test: /\.css?$/,
        use: ['style-loader', 'css-loader'],
      },
      {
        test: /\.(ico|png|jpg|jpeg|gif|svg|woff|woff2|ttf|eot)(\?v=[0-9]\.[0-9]\.[0-9])?$/,
        loader: 'url-loader',
        options: {
          name: '[hash].[ext]',
          limit: 1000000,
        },
      },
    ],
  },
  plugins: [
    new webpack.ProvidePlugin({
      $: 'jquery/src/jquery',
      jquery: 'jquery/src/jquery',
    }),
    new DtsBundleWebpack({
      name: 'dms-video-player',
      externals: true,
      main: path.resolve(__dirname, './dist/dms-video-player.min.js'),
      out: path.resolve(__dirname, './dist/dms-video-player.d.ts'),
    }),
  ],
  devServer: {
    clientLogLevel: 'info',
    contentBase: [path.join(__dirname, 'public')],
    publicPath: '/dist/',
    port: 8081,
    watchContentBase: true,
  },
};